package main.java.clueless.gamelogic;

/**
 * Used to represent the four invalid locations on the board.
 * 
 * @author matthewsobocinski
 */
public class InvalidLocation extends BoardLocationEntity {
	public InvalidLocation(String name, String abbreviation, Location location) {
		super(name, abbreviation, location);
	}
}
